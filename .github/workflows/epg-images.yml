name: Update EPG Titles DB

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}   # 👈 your PAT with repo permission

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run updater script
        run: node update-epg-db.mjs

      - name: Commit & push changes
        run: |
          git config user.name "kunani1"
          git config user.email "kunanisaitarun55@gmail.com"
          git add data || echo "Nothing to add"
          git commit -m "Update EPG title→image DB" || echo "No changes"
          git push origin HEAD:main
